## we recently built a chat bot which also had short term memory ; but that chat bot had a big flow that , it didn't had any ui

# Plan of action

- There would be two parts "Backend" -> Langgraph workflow and "Frontend" -> Steamlit website

## first learn how to make chat interface using streamlit and then see how to integrate it with langgraph

- Streamlit as a component -> **chat_message** -> which is used for that box which showing user and ai response

- and we have **chat input box** -> from where user give their response

## so first we try to build these two components ;; for running streamlit we have to give command -> 'streamlit run streamlit_file_name'
