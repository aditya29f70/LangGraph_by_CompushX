## We have been building a chatbot and adding different features like

1. basic, 2. ui, 3. streaming , 4. resume chat, 5. obserbability, 6. tools, 7. mcp ; now going to add RAG concept -> it will become RAG chat-bot

## RAG in langGraph:

- langgraph there are multiple ways to implement rag (the way we will define rag is very simple -> **we will define rag as tool**) and will tread as we tread other tool

* in agentic workflow usually rag is implemented as tool
